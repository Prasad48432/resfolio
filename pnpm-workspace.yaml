packages:
  - "apps/*"
  - "packages/*"
  # Planned layers (docs/architecture 03, 05, 06) — globs registered ahead
  # of time so new packages resolve without workspace-config surprises.
  - "domains/*"
  - "templates/*"

{
  "name": "resfolio",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,md}\"",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.10.5",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.9"
  },
  "pnpm": {
    "peerDependencyRules": {
      "//": "@better-auth/cli versions independently of better-auth (lib 1.6.x, CLI still on 1.4.x) and is codegen-only — it hoists a stale @better-auth/core + drizzle-orm@0.41.0 whose peers our newer versions technically don't satisfy. The runtime auth adapter takes its drizzle client from @resfolio/database (0.45.2), so these are benign. Widen the accepted peer ranges to the versions actually installed.",
      "allowedVersions": {
        "@better-auth/core>@better-fetch/fetch": "1.3.1",
        "@better-auth/core>better-call": "1.3.7",
        "@better-auth/core>kysely": "0.29.3",
        "better-auth>drizzle-orm": "0.41.0",
        "@better-auth/drizzle-adapter>drizzle-orm": "0.41.0"
      }
    }
  }
}
